<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="file.majing.community.mapper.QuestionExtMapper">
    <resultMap id="BaseResultMap" type="file.majing.community.model.Question">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Thu Apr 09 22:18:33 CST 2020.
        -->
        <id column="ID" jdbcType="BIGINT" property="id" />
        <result column="TITLE" jdbcType="VARCHAR" property="title" />
        <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate" />
        <result column="GMT_MODIFIED" jdbcType="BIGINT" property="gmtModified" />
        <result column="CREATOR" jdbcType="BIGINT" property="creator" />
        <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount" />
        <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount" />
        <result column="LIKE_COUNT" jdbcType="INTEGER" property="likeCount" />
        <result column="TAG" jdbcType="VARCHAR" property="tag" />
    </resultMap>
    <select id="selectRelated" parameterType="file.majing.community.model.QuestionExample" resultMap="BaseResultMap">
        select * from QUESTION where id!= #{id} and tag regexp #{tag} order by GMT_CREATE desc
    </select>
    <update id="incView" parameterType="file.majing.community.model.Question">
        update QUESTION set VIEW_COUNT = VIEW_COUNT + #{viewCount,jdbcType=INTEGER}
        where ID = #{id}
    </update>

    <update id="incCommentCount" parameterType="file.majing.community.model.Question">
        update QUESTION set COMMENT_COUNT = COMMENT_COUNT + #{commentCount,jdbcType=INTEGER}
        where ID = #{id}
    </update>
    <select id="countBySearch" parameterType="file.majing.community.dto.QuestionQueryDTO" resultType="java.lang.Integer">
        select count(ID) from QUESTION
         <where>
             <if test="search != null">
                 and title regexp #{search}
             </if>
         </where>
    </select>
    <select id="selectBySearch" parameterType="file.majing.community.dto.QuestionQueryDTO" resultMap="BaseResultMap">
        select * from QUESTION
         <where>
             <if test="search != null">
                 and title regexp #{search}
             </if>
         </where>
        order by gmt_create desc limit #{page},#{size}
    </select>
</mapper>